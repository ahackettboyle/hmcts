---

- hosts: webservers

  roles:
    - common
    - nginx

- hosts: database


  roles:
    - role: postgres
    - role: postgresql_objects


- hosts: loadbalancer

  roles:
    - { role: common, when: "env == 'prod'" }
    - { role: haproxy, when: "env == 'prod'" }
